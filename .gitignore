# 依赖（用 npm install 重新生成，不上传）
node_modules/
app/node_modules/

# 构建产物（用 npm run build 重新生成）
dist/
dist-deploy/
app/dist/
*.local

# 日志
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 环境变量（可能含密钥，绝不上传）
.env
.env.*
!.env.example

# 编辑器/系统文件
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 本项目的大文件 / 备份 / 下载残留（不上传）
portfolio-project.tar.gz
zibJ5VZd
*.tar.gz
